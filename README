1 工具说明
该工具功能主要包括以下几个部分：
（1） 用于检测系统安装OS后启动过程中是否存在问题；
（2） 汇总系统中CPU的个数，socket的个数，DDR内存的总容量（输出的数字会稍微比实际值少一点点），以及插入的磁盘的总个数和每个磁盘的大小等信息。

2 如何使用
（1）安装完系统后，重启系统，以root登录系统，打开终端
（2）将这个system-check.tar通过U盘或者网络等方式copy到系统上的某一个目录，并解压
	# tar xf system-check.tar
（3）进入到这个目录
	# cd system-check
（4）运行测试脚本
	# ./runasroot.sh
（5）根据实际情况检查输出信息是否有异常，比如dmesg中是否有错误log，CPU，Socket，DDR内存总数量，以及磁盘等信息是否正确。

3 特殊说明
由于一些特殊情况，在检查系统启动log时可能会误报一些错误，或者漏报一些无特殊关键字的错误，所以这里准备了两个特殊的文件
exception.rec
normal.rec
其中exception.rec用于记录错误log的关键字，一行代表一个错误
normal.rec用于记录误报log的关键字，一行代表一个错误
根据实际上情况更新这两个文件就可以了。
